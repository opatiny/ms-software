#pragma once
#include "../globalConfig.h"
void printWifiHelp(Print* output);

void processWifiCommand(char command,
                             char* paramValue,
                             Print* output) ;